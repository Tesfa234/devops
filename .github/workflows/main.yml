name: DevOps Pipeline

on: [push]

jobs:
  pipeline:
    runs-on: ubuntu-latest

    steps:
      - name: Build
        run: echo "🔨 Building..."

      - name: Test
        run: echo "🧪 Testing..."

      - name: Deploy
        run: echo "🚀 Deploying..."
